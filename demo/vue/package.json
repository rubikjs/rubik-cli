{
  "name": "cli-vue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-cdn": "cross-env CDN_ENV=true yarn run cli build",
    "build-no-hash": "cross-env NO_HASH_ENV=true yarn run cli build",
    "build": "yarn run cli build",
    "start": "yarn run cli serve",
    "lint": "yarn run cli lint",
    "stylelint": "yarn run cli stylelint",
    "cli": "node ../../bin/main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vue": "^2.6.10",
    "vue-router": "^3.0.7"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "sass-resources-loader": "^2.0.1"
  }
}
